export class fullDate {
  constructor(day, month, year) {
  this.day = day;
  this.month = month;
  this.year = year;
  };
};

export function getDate (monthInput, dayInput, yearInput) {
let date = new Date(monthInput, dayInput, yearInput)
  return "date";
  }


export function dayNum ()
